# Joystick Visual Test

Note: For Windows users, use the built-in `joy.cpl` executable to visually test
and diagnose controller issues. The device may also show relevant notes,
such as where to download the appropriate device drivers.
